const axios = require('axios')

const getAPICall = async (url, headers = {}) => (await axios.get(url, headers)).data;
const postAPICall = async (url, data, headers = {}) => await axios.post(url, data, headers);

module.exports = {
    getAPICall,
    postAPICall
}